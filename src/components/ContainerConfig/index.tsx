import React from 'react'
import styled from '@emotion/styled'

export default () => {
  return <Config>11</Config>
}

const Config = styled.div`
  width: 400px;
  height: 100%;
  background-color: #fff;
  position: relative;
  z-index: 9;
  box-shadow: 0 2px 12px 0 rgba(0, 0, 0, 0.1);
`
